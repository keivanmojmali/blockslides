{
  "name": "autoartifacts-monorepo",
  "version": "0.0.1",
  "description": "AutoArtifacts monorepo - ProseMirror-based slide editor",
  "private": true,
  "scripts": {
    "build": "pnpm --filter '@autoartifacts/*' --recursive run build",
    "dev": "pnpm --filter '@autoartifacts/*' --recursive run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
